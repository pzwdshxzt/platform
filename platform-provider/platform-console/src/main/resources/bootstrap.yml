spring:
  profiles:
    active: dev,shiro,mysql

  cloud:
    config:
      name: spring-cloud-config-client
      profile: dev
      uri: http://localhost:8888/,http://localhost:8889/
      label: master